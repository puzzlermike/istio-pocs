apiVersion: networking.istio.io/v1alpha3
kind: EnvoyFilter
metadata:
  name: filter-local-ratelimit-jwt-dynamic-tenancy
  namespace: ingress
spec:
  workloadSelector:
    labels:
      gateway.networking.k8s.io/gateway-name: gateway

  configPatches:
    - applyTo: HTTP_FILTER
      match:
        context: GATEWAY
        listener:
          filterChain:
            filter:
              name: envoy.filters.network.http_connection_manager
              subFilter:
                name: envoy.filters.http.router

      patch:
        operation: INSERT_BEFORE
        value:
          name: envoy.filters.http.local_ratelimit
          typed_config:
            "@type": type.googleapis.com/udpa.type.v1.TypedStruct
            type_url: type.googleapis.com/envoy.extensions.filters.http.local_ratelimit.v3.LocalRateLimit
            value:
              stat_prefix: jwt_dynamic_tenant_local_rate_limit

    - applyTo: HTTP_ROUTE
      match:
        context: GATEWAY
        routeConfiguration:
          vhost:
            name: "echo.istio-pocs-001:80"
            route:
              action: ANY

      patch:
        operation: MERGE
        value:
          typed_per_filter_config:
            envoy.filters.http.local_ratelimit:
              "@type": type.googleapis.com/udpa.type.v1.TypedStruct
              type_url: type.googleapis.com/envoy.extensions.filters.http.local_ratelimit.v3.LocalRateLimit

              value:
                stat_prefix: jwt_dynamic_tenant_local_rate_limit

                token_bucket:
                  max_tokens: 4
                  tokens_per_fill: 4
                  fill_interval: 60s

                rate_limits:
                  - actions:
                      - request_headers:
                          header_name: x-jwt-tenant
                          descriptor_key: tenant

                descriptors:
                  - entries:
                      - key: tenant
                    token_bucket:
                      max_tokens: 4
                      tokens_per_fill: 4
                      fill_interval: 60s

                max_dynamic_descriptors: 1000

                filter_enabled:
                  default_value:
                    numerator: 100
                    denominator: HUNDRED

                filter_enforced:
                  default_value:
                    numerator: 100
                    denominator: HUNDRED

                always_consume_default_token_bucket:
                  value: false
